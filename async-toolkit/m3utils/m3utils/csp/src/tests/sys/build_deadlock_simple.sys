// Two processes both sending on the same channel, no receiver.
// Deadlock: neither can complete its send.
system DeadlockSimple;
  var c : channel(32);

  process Sender = "deadlock_send_send.csp"
    port out R : channel(32);

begin
  var s1 : Sender(R => c);
  var s2 : Sender(R => c);
end.
